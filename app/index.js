/**
 * Application entry point
 */

// Load styles
import '../assets/styles/index.scss'

// START YOUR JS CODE HERE

const body = document.querySelectorAll('h3')

;(function test() {
  switch (true) {
    case true:
      return true
      break
    default:
  }
})()
